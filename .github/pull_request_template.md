## Summary

-

## Test plan

- [ ]

## Architecture (AD-1)

- [ ] Route and MCP handlers call domain services only — no inline Drizzle queries or direct R2 client usage in `apps/api/src/routes` or `apps/api/src/mcp`.
