import { Title } from "@mantine/core";

export default function AccountSettingsPage() {
  return (
    <main style={{ padding: "2rem" }}>
      <Title order={2}>Account settings</Title>
      <p>Settings hub scaffold — E3-S1.</p>
    </main>
  );
}
