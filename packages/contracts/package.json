{
  "name": "@usebugreport/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun run scripts/validate-routes.ts && tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "bun test scripts",
    "test:parity": "bun test tests/parity.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "drizzle-orm": "^0.38.0",
    "typescript": "^5.8.0"
  }
}
