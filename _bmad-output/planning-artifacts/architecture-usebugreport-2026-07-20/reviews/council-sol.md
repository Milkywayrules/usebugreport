**NO_WITH_NOTES**:
Do not begin epic/story creation from this version unchanged. The layered-monolith direction is sound, and I accept the decisions in B, C, and D, but A fails because two production-critical flows are internally inconsistent and coverage of all 26 FRs is asserted rather than demonstrated. Revise the architecture, then it is suitable as the implementation basis.

**Reasoning**:

- A — reject the current architecture as the epic/story baseline. It names all 11 launch gates at a high level, but it does not provide a trustworthy FR-1..FR-26 traceability matrix with an implementation owner and verification mechanism for every consequence. Material gaps include Free/Pro workspace-count enforcement and mandatory first-workspace onboarding from FR-8, API-key expiry/rotation and last-used behavior from FR-22/FR-23, and an explicit session-auth route/contract for launch-gate human comments in FR-26.
- A — the ingest handoff is not viable as written. For inline payloads, the API acknowledges after enqueue while the worker later uploads bytes to R2, but the document never defines a durable location for those bytes. Putting up to 32 MB in BullMQ/Redis contradicts the 12 GB single-VPS constraint and becomes catastrophic with the stated 100 concurrent jobs per workspace. Define one durable protocol: preferably direct presigned R2 upload followed by a small idempotent finalize job, with inline ingest restricted to a deliberately small cap and streamed to R2 before acknowledgement. BullMQ jobs must contain object references, not replay blobs.
- A — the GDPR workflow is self-invalidating. `deletion.postgres_purge` deletes the organization and its dependent `deletion_jobs` row, then later steps attempt Redis purge, audit completion, terminal-state persistence, polling, and owner email. Preserve a deletion tombstone/control record outside the tenant cascade, snapshot the owner notification address before purge, perform all external-store cleanup, write the audit result, and only then remove tenant rows. Define retry/idempotency behavior for every step.
- A — R2 lifecycle rules “per `{orgId}/` with tier-specific expiration” need correction. Dynamic per-workspace tier changes are not a dependable lifecycle-rule model at SaaS cardinality. Treat `report_blobs.expires_at` plus the durable retention sweeper as the authority; use coarse bucket lifecycle rules only as a safety backstop. Specify upgrade/downgrade semantics and orphan reconciliation.
- A — outbound side effects need race-safe state, not only “if `linear_issue_id` exists.” Concurrent Linear pushes can create duplicates before either stores the ID. Use a durable operation/outbox row with a unique report/action key and explicit pending/succeeded/failed states. Webhook registration must also include SSRF controls beyond HTTPS-only: reject private, loopback, link-local, and metadata destinations and revalidate resolved addresses on delivery.
- B — accept 15-minute presigned R2 GET URLs for v1; do not proxy replay bytes through the API. On one VPS, proxying creates avoidable bandwidth and availability coupling. The residual bearer-URL revocation lag is bounded and acceptable for v1 if the bucket is private, manifests are authorized on every issuance, responses use `Cache-Control: private, no-store`, object keys are unguessable, raw R2 keys are never returned, URLs are redacted from logs, and membership/key revocation prevents new manifests immediately. R2 hostname exposure is not a security boundary; use a custom download hostname later if branding requires it.
- B — do not expose replay URLs in MCP tool results unless a launch requirement actually needs replay-byte access. Model/tool transcripts are a larger leak surface than the authenticated web viewer, and FR-15 does not require a replay-manifest MCP tool.
- C — keep the separate worker container. CPU-heavy compression, R2 transfer, webhook retries, retention, and deletion should not share an event loop or restart lifecycle with latency-sensitive REST/MCP/auth. The extra Bun process is a reasonable memory cost; define container memory limits/reservations, worker concurrency based on measured RSS, graceful shutdown, and queue-drain behavior. Folding the worker into API would save little while worsening failure isolation.
- D — accept the explicit surface registry and contract tests. This is justified control-plane code for a launch-gate parity promise, and it avoids accidental exposure from `elysia-mcp`. Keep the registry declarative and small; validate registrations rather than building a bespoke code generator unless repetition proves painful.
- D — parity tests must exercise the real REST HTTP and MCP Streamable HTTP adapters, including auth scopes, errors, filters, cursor pagination, and serialization, not merely call both handler functions in-process. Deep equality should be schema-aware and normalize only documented transport envelopes.

**Additional from me**:

- Make the required architecture revision narrowly: add a launch-gate/FR traceability table, repair ingest durability, repair deletion ordering, declare retention authority, and make outbound effects race-safe. No microservice split or new infrastructure is needed.
- Add explicit invariants that BullMQ payloads contain references only, tenant deletion control records survive tenant deletion, every tenant query includes `organization_id`, and raw blob keys/presigned URLs are never logged.
- Add failure-path tests for duplicate ingest/finalize, worker crash after R2 PUT but before Postgres commit, concurrent Linear push, expired/revoked membership with an old replay URL, retention retry, and deletion resumed after each step.
- After those changes, my expected vote is YES_WITH_NOTES; the core layered monolith, separate worker, presigned replay delivery, and shared REST/MCP service architecture are appropriate for v1.

---

# Re-review verdict (2026-07-20, post-revision)

**YES_WITH_NOTES**:
Approve the revised architecture as the basis for epic/story creation. I re-read both revised documents and verified every mandatory fix from my NO_WITH_NOTES verdict is genuinely applied in substance, not merely name-checked. The two blocking inconsistencies — ingest durability and the self-invalidating GDPR cascade — are correctly repaired. Remaining findings are minor and can be resolved during story writing.

**Reasoning**:

- Ingest durability (verified): AD-4 and §4 now define a coherent durable protocol — presigned PUT direct to R2 followed by `POST /capture/complete` enqueueing an idempotent `ingest.finalize` job, with the inline path capped at 1 MB and streamed to R2 before the HTTP ack. The "BullMQ payloads contain references only" invariant appears in the spine invariants table, AD-4, and §4 with a CI enforcement mechanism. The old contradiction (32 MB bodies implicitly parked in Redis) is gone.
- GDPR deletion (verified): `deletion_tombstones` is a real table with no FK to `organizations`, owner email and slug snapshotted at enqueue, `last_completed_step` resume pointer, and §9 reorders steps so Postgres tenant purge runs last after external purge and terminal audit. Each step declares its idempotency mechanism. The cascade can no longer delete its own control record.
- Retention authority (verified): §7 names `report_blobs.expires_at` plus the daily sweeper as the single authority, demotes R2 lifecycle to a global 120-day backstop (explicitly not per-org/per-tier), and specifies upgrade/downgrade recompute via `UsageService.recomputeRetention` plus bidirectional orphan reconciliation.
- Race-safe outbound effects (verified): `integration_operations` outbox with `UNIQUE (report_id, action)` and pending/succeeded/failed states replaces the naive `linear_issue_id` check; jobs carry `{ operationId }` references only. SSRF controls in §8 reject loopback, RFC1918, link-local, metadata, and CGNAT ranges with revalidation at delivery time and redirects disabled.
- Traceability (verified): §18 maps all of FR-1..FR-26 to an architecture anchor and a verification mechanism, including the previously missing FR-8 workspace-limit/onboarding gate (now AD-11 plus `onboarding-gate.ts` hard redirect), FR-22/FR-23 key lifecycle, and the FR-26 session-auth comment route `POST /api/web/reports/:id/comments` wired to the shared `CommentService`.
- Parity testing (verified): AD-2 and §5.3 now require real HTTP through both adapters — `fetch()` against `/api/v1/*` and an MCP Streamable HTTP client against `/mcp` — asserting scopes, error envelopes, Free-tier write rejection, filters, and cursor pagination, with in-process handler calls explicitly disallowed.
- Operations (verified): container memory budget sums to ~8.1 GB of limits with ~4 GB headroom on the 12 GB VPS; worker concurrency is env-driven with an RSS probe and a graceful SIGTERM drain with a matching Coolify grace period.

**Additional from me**:

- Minor: AD-6 still says MCP tools may "return URLs in tool results," but no replay-manifest operation exists in the surface registry — so nothing actually exposes presigned URLs to agents. Delete that clause during story writing to prevent a future story from reintroducing it casually.
- Minor: §5.5 defines conflict behavior only for `status=succeeded`; specify the `pending` (return in-progress, do not enqueue again) and `failed` (allow explicit retry that flips the row back to pending) branches in the E7 story.
- Minor: the inline ingest path now performs an R2 PUT before the 202 ack, which is in tension with the "< 200ms p95 ack" figure inherited from the PRD; for ≤1 MB from Frankfurt this is likely fine, but the E2 story should measure it and, if needed, relax the p95 target for the inline path only rather than weakening durability.
- Minor: SSRF revalidation at delivery should pin the connection to the validated resolved IP (resolve once, connect to that address) to close the DNS-rebinding TOCTOU window; one sentence in the E8 story suffices.
- Minor: add a stale-`pending` report sweep (presign issued but `complete` never called) so abandoned ingests do not accumulate as pending rows forever; the orphan reconciliation covers the R2 side but not the Postgres `ingest_status=pending` side.
- None of these notes block epic/story creation; proceed to `bmad-create-epics-and-stories` citing AD IDs and §18.
