---
scope: usebugreport v1.0 platform
purpose: build-substrate
altitude: initiative
updated: 2026-07-20T06:25
---

- (decision) paradigm=layered monolith with shared domain services; REST and MCP are transport adapters only
- (decision) monorepo apps/web apps/api apps/worker; packages/db packages/contracts packages/config packages/capture-core packages/sdk
- (decision) parity enforced via packages/contracts surface registry + bun test contract tests; no inline MCP query logic
- (decision) ingest presign PUT + ingest.finalize refs-only; inline ≤1MB stream-before-ack
- (decision) replay served via short-lived R2 presigned URLs from ReportService.getReplayUrls; no VPS blob proxy
- (decision) EU hosting on Contabo VPS Frankfurt; single region v1
- (decision) Postgres FTS with generated tsvector column on reports.search_vector; GIN index
- (decision) GDPR deletion via deletion_tombstones (no org FK) + ordered BullMQ steps; resume from last_completed_step
- (decision) webhook delivery via webhooks queue; HMAC SHA256; 5 attempts exponential backoff
- (decision) usage counters in workspace_usage_monthly table; Redis sliding window for rate limits
- (constraint) stack fixed per PRD addendum; no microservices k8s event sourcing
- (version) Bun 1.2.x runtime; Next.js 15 App Router; Elysia 1.x; Drizzle ORM; Mantine 7.x; MCP SDK v1.x pinned
- (assumption) separate worker container shares packages/services with api; same Docker image different CMD
- (event) spine drafting started headless mode
- (event) spine finalized
- (revision) design review 2-1 approval — mandatory fixes applied 2026-07-20T06:25
- (decision) BullMQ payloads references-only; presign PUT + ingest.finalize; inline ≤1MB stream-before-ack
- (decision) deletion_tombstones outside org FK cascade; ordered purge with last_completed_step resume
- (decision) report_blobs.expires_at + retention.sweep single authority; R2 lifecycle 120d global backstop only
- (decision) integration_operations outbox UNIQUE(report_id, action) for race-safe Linear push
- (decision) AD-11 tier limits at service boundary per PRD §9 four-tier table
- (decision) FR-26 web comments via POST /api/web/reports/:id/comments session route
- (decision) parity tests real HTTP REST + MCP Streamable HTTP e2e
- (decision) worker memory budget 2GB limit; RSS-based concurrency; SIGTERM queue drain 120s
- (decision) webhook SSRF HTTPS-only + private IP block with delivery-time revalidation
- (artifact) §18 FR-1..FR-26 traceability matrix added to architecture.md
- (revision) reviewer minor fixes 2026-07-20 — five surgical doc edits before story freeze: (1) AD-6 — removed MCP tool-result URL clause; presigned replay URLs never in MCP; no replay-manifest in surface registry; (2) §5.5 — integration outbox conflict branches for pending (in-progress, no re-enqueue) and failed (explicit retry → pending + enqueue once); (3) §4/AD-4 — inline ingest E2 must measure ack latency; relax inline p95 only if R2 round-trip breaks <200ms target; durability never weakened; (4) §8 SSRF — delivery-time resolve-once, validate IP, pin connection (connect by IP + Host/SNI); redirects disabled; (5) §4/§7 — stale pending sweep: ingest_status=pending >24h (presign without complete) deleted by retention/reconciliation job + best-effort orphan R2 delete
